一、金融智能体整体架构（四层架构）
UI展示层
支持Web端和移动端统一入口，通过端流式网关接入后端。
业务服务层
承载传统业务逻辑（用户管理、卡片渲染、运营管理等），基于Java技术栈实现。
智能服务层
专为AI能力设计（Python工程），包含四车间智能体架构（意图识别→策划→召回→表达）和算法服务依赖。
金融供给层
提供三类核心“手”能力：
金融知识：指标、资讯、FAQ等（KBASE存储）。
金融工具：封装行内原子工具（如KYC、持仓查询、产品对比选品）。
LUI卡片：图文并茂的交互形式（依托Mpass产品）。
基础设施层
云原生部署（CNstack）、智算平台、中间件及监控运维体系。
⚙️ 二、核心链路实现（用户问题处理流程）
预处理环节
多人改写：指代消解（如“他能买吗”→前文基金名称）、错字订正、语病修正、外语翻译。
内容安全拦截：通过“天剑”系统过滤暴政黄赌毒、提示词越狱攻击（如“教我做炸弹”）。
四车间架构
第一车间（意图识别）
识别问题主体与动作（如“基金产品信息查询”），并行触发高优FAQ短路链路（如监管问题直接返回标准答案）。
第二车间（策划）
通过Function Call筛选工具（如“基金评价解读工具”），结合提槽模型提取参数（如基金代码）。
第三车间（召回）
并行调用金融工具、知识库、图片信息，优化上下文工程降低表达模型压力。
第四车间（表达）
动态拼接供给与提示词片段，调用大模型生成图文回答，同步触发内容安全检查与推荐问生成。
特色链路
智能选品：自然语言→ChatBI模块→NL2SQL转换→ES查询→业务逻辑过滤（如用户KYC）。
澄清反问：当产品指代不清时（如“宁银日薪”），按业务规则返回3款候选产品卡片供用户选择。
🔧 三、工程化核心实践
开发框架
采用财宝开源的Agent Universe框架，支持多Agent范式、池化管理、配置化编排。
提示词管理
自研平台统一管理360+提示词片段，支持批量导入导出、热更新、版本对比与回滚。
调试与调优
Debug平台：通过Trace ID穿透四车间各环节，查看输入输出、耗时与提示词。
调优SOP：
检查工具调用列表→校验工具入参/出参→排查知识工程/提槽/业务逻辑问题→优化表达提示词。
上下文工程优化
结构化供给：用表格/JSON替代文本（避免张冠李戴）。
预计算结果：提前统计持仓数量、单位换算（如“137亿”而非“13700000000”）。
消歧处理：明确主体（“您问的是明星天天牛5号”）、时间（交易日判断）、空值原因（“0%因产品未满7天”）。
动态剪枝：按优先级裁剪冗余内容（如重复卡片、无用字段）。
⚡️ 四、性能优化成果
首Token响应时间：从单并发14秒、50并发20秒，优化至50并发下8秒内。
关键策略：
流程重塑：高优FAQ异步检测，避免阻塞主链路。
并行召回：搜索类工具提前启动（如全网搜无需等待提槽）。
模型精简：提槽模型从7B→4B（性能提升45%），NL2SQL模型压缩（选品链路耗时8.3秒→3秒）。
📈 五、总结与经验沉淀
三个建设阶段
复刻蚂蚁端到端架构→升级四车间支持全场景→评测驱动优化与方法论沉淀。
AI工程思维转变
评测驱动：准确率是核心指标，需持续迭代测试。
先工程后模型：优先优化上下文工程（如提示词、供给结构），而非直接调整模型。
未来方向
标准化高优FAQ能力套件，支持快速复制。
金融工具与知识通过MCP市场开箱即用，对外赋能。
